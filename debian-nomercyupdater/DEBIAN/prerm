#!/bin/sh
set -e

# Stop and disable service
if [ -n "$SUDO_USER" ]; then
su - $SUDO_USER -c "systemctl --user stop nomercyupdater.service" || true
su - $SUDO_USER -c "systemctl --user disable nomercyupdater.service" || true
fi

exit 0
